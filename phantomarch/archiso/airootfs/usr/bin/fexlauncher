#!/bin/bash
# FexLauncher — Native PyQt6 Minecraft Launcher
if [ "$1" = "--cli" ]; then
    exec python3 /opt/fexlauncher/fexlauncher.py "${@:2}"
else
    exec python3 /opt/fexlauncher/fexlauncher_gui.py "$@"
fi
